import React from 'react'

export default function ProfileCardEditor({loggedInUser}) {
    // const [editPost, setEditPost] = useState(initialForm)
    // const {posts} = user

    // let intitialForm={
    //     hed: 
    // }

  return (
    <div>
      {/* <form className="card-editor" >
            <input 
                type='text' 
                name='hed' 
                placeholder='headline'
                value={newPost.hed} 
                onChange={handlePostChange} 
            />
            <input 
                type='text' 
                name='dek' 
                placeholder='description'
                value={newPost.dek} 
                onChange={handlePostChange} 
            />
            <textarea
              name='content'
              id='card-editor-body'
              value={newPost.content}
              onChange={handlePostChange}
            />
        <button type="submit" onSubmit={handlePublish}>Publish</button>
      </form> */}
    </div>
  )
}
